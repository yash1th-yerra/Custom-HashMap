Implementing HashCard aka HashMap of java collections framework from scratch(custom hashmap).

Requirements:
list of buckets(key value pair class)
arrays to store buckets 
linkedlist to avoid hash collosions